package hn.lenguajes.creditos.servicios;

public class PrestamoServicio {

    /*
     * public Prestamo crearPrestamo(String dni, Prestamo nvoPrestamo){   
        Cliente cliente = nvoPrestamo.getCliente();     
        if(this.clienteRepositorio.existsById(dni) ){
            if(clienteRepositorio)
            nvoPrestamo.setCliente(this.clienteRepositorio.findById(dni).get());
            return this.prestamoRepositorio.save(nvoPrestamo);
        }

        return null;
    }
     */
    
}
